# cz-open-data-render

Deploy monorepo for Frantisek five CZ open-data apps on Render (wave 1).

Services (see root render.yaml): deskaradar, sitepack, spectrum, wastegate, capexpulse.

All bind 0.0.0.0 and use PORT. Health check path: /healthz.

Frankfurt region, free plan. No remote configured yet.
